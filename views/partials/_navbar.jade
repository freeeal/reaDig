nav.navbar.navbar-inverse.navbar-custom
  .container-fluid
        .navbar-header
          .navbar-brand
            - if (!user) {
              a(href='/auth')
                img(src='../../images/reaDig-logo.png', style='max-width:133px; margin-top:-80px; margin-left:-35px') 
              - }
            - else {
              a(href='/profile')
                img(src='../../images/reaDig-logo.png', style='max-width:133px; margin-top:-80px; margin-left:-35px')
              - }
                
        div
          ul.nav.navbar-nav
            li
              if (user)
                a(href='/home') home
            |         
            li
              if (user)
                a(href='/profile') profile
            |         
            li
              if (user)
                a(href='/friends') friends
            |           
            li
              if (user)
                a(href='/reviews') reviews
          |       
          ul.nav.navbar-nav.navbar-right      
            if (!user)
              li
                a(href='/auth/login')
                  span.glyphicon.glyphicon-log-in
                  |  login
            else
              li
                a(href='/auth/logout')
                  span.glyphicon.glyphicon-log-out
                  |  logout
